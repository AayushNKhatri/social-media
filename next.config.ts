import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  //productionBrowserSourceMaps:true, //this solves unbound breakpoint problem
};

export default nextConfig;
